#! /bin/sh

# libstatgrab
cd libstatgrab-0.7
if [ ! -f libstatgrab-configure.gnu ]
then
  cp ../patches/libstatgrab-configure.gnu .
  autoconf
fi
cd ..

aclocal \
&& autoheader \
&& libtoolize \
&& automake --gnu --add-missing \
&& autoconf
